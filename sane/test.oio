TEST

local channel STR: err(string@{h});
local channel INT: err(int@{h});

var c: map((int@{h} * int@{h})@{h})@{} = makemap([(1,2); (2,3); (3,4)]);
var d: err(int@{h}) = 0;

START@{} (ignored: int@{}) {
    c[[1]] = 100;
    d = c[[1]];
    output(INT, d);
    output(STR, "ok");

    exit();
}