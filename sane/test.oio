TEST

local channel STR: string@{h};
local channel INT: err(int)@{h};

var a: int@{h} = 1;
var c: int@{} = 3;


START@{} (ignored: int@{}) { 
    oblif a then
        c ?= 10;
    else
        c ?= 20;

    output(INT, c);
    output(STR, "ok");
    exit();
}
