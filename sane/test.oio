TEST

local channel STDOUT: err(string@{h});
local channel INT: int@{h};

var c: path(string@{h})[1]@{h} = onil(1);
var d: err(string@{h}) = "";

START@{} (ignored: int@{}) {
    // c = oram("ABCDEFGH", 1);
    d = *c;

    output(STDOUT, d);

    exit();
}