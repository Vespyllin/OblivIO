TEST

local channel STDOUT: string@{h};

var c: string@{h} = "Cad";
var a: ptr(string@{h})@{h} = nil;
var b: path(string@{h})@{h} = oram c;

START@{} (ignored: int@{}) {
   a = alloc c;
   *a ?= "A";
    
   output(STDOUT, *a);
   // output(STDOUT, "ok");

    exit();
}